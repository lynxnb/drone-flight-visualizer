#include "renderer.h"
#include <glm.hpp>
#include <array>

namespace dfv::vulkan {
    Renderer::Renderer(const VulkanContext &context) : vkContext(context) {

    }

    void Renderer::renderDrone(int a) {

    }
}

